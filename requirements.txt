streamlit
pandas
python-dotenv
google-generativeai
requests # For making HTTP requests to PubMed API
# Add other specific versions if known, e.g., streamlit==1.20.0
# Consider adding versions for all packages for reproducible deployments. 