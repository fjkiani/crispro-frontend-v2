import React from 'react';
import { Box, Typography, Select, MenuItem, FormControl, InputLabel } from '@mui/material';

const GauntletControl = ({ selectedEnvironment, onEnvironmentChange }) => {
    return (
        <Box sx={{ mb: 3, p: 2, border: '1px dashed grey', borderRadius: 2 }}>
            <Typography variant="h6" gutterBottom>Digital Twin Environment</Typography>
            <FormControl fullWidth>
                <InputLabel id="digital-twin-select-label">Select Simulation Environment</InputLabel>
                <Select
                    labelId="digital-twin-select-label"
                    id="digital-twin-select"
                    value={selectedEnvironment}
                    label="Select Simulation Environment"
                    onChange={onEnvironmentChange}
                >
                    <MenuItem value={"glioblastoma_high_angiogenesis"}>Glioblastoma, High Angiogenesis</MenuItem>
                    <MenuItem value={"pancreatic_cancer_kras_mutant"}>Pancreatic Cancer, KRAS Mutant</MenuItem>
                    <MenuItem value={"healthy_control"}>Healthy Control</MenuItem>
                </Select>
            </FormControl>
        </Box>
    );
};

export default GauntletControl; 
 
 
 
 
 
 
 
import { Box, Typography, Select, MenuItem, FormControl, InputLabel } from '@mui/material';

const GauntletControl = ({ selectedEnvironment, onEnvironmentChange }) => {
    return (
        <Box sx={{ mb: 3, p: 2, border: '1px dashed grey', borderRadius: 2 }}>
            <Typography variant="h6" gutterBottom>Digital Twin Environment</Typography>
            <FormControl fullWidth>
                <InputLabel id="digital-twin-select-label">Select Simulation Environment</InputLabel>
                <Select
                    labelId="digital-twin-select-label"
                    id="digital-twin-select"
                    value={selectedEnvironment}
                    label="Select Simulation Environment"
                    onChange={onEnvironmentChange}
                >
                    <MenuItem value={"glioblastoma_high_angiogenesis"}>Glioblastoma, High Angiogenesis</MenuItem>
                    <MenuItem value={"pancreatic_cancer_kras_mutant"}>Pancreatic Cancer, KRAS Mutant</MenuItem>
                    <MenuItem value={"healthy_control"}>Healthy Control</MenuItem>
                </Select>
            </FormControl>
        </Box>
    );
};

export default GauntletControl; 
 
 
 
 
 
 
 
import { Box, Typography, Select, MenuItem, FormControl, InputLabel } from '@mui/material';

const GauntletControl = ({ selectedEnvironment, onEnvironmentChange }) => {
    return (
        <Box sx={{ mb: 3, p: 2, border: '1px dashed grey', borderRadius: 2 }}>
            <Typography variant="h6" gutterBottom>Digital Twin Environment</Typography>
            <FormControl fullWidth>
                <InputLabel id="digital-twin-select-label">Select Simulation Environment</InputLabel>
                <Select
                    labelId="digital-twin-select-label"
                    id="digital-twin-select"
                    value={selectedEnvironment}
                    label="Select Simulation Environment"
                    onChange={onEnvironmentChange}
                >
                    <MenuItem value={"glioblastoma_high_angiogenesis"}>Glioblastoma, High Angiogenesis</MenuItem>
                    <MenuItem value={"pancreatic_cancer_kras_mutant"}>Pancreatic Cancer, KRAS Mutant</MenuItem>
                    <MenuItem value={"healthy_control"}>Healthy Control</MenuItem>
                </Select>
            </FormControl>
        </Box>
    );
};

export default GauntletControl; 
 
 
 
 
 
 
 
import { Box, Typography, Select, MenuItem, FormControl, InputLabel } from '@mui/material';

const GauntletControl = ({ selectedEnvironment, onEnvironmentChange }) => {
    return (
        <Box sx={{ mb: 3, p: 2, border: '1px dashed grey', borderRadius: 2 }}>
            <Typography variant="h6" gutterBottom>Digital Twin Environment</Typography>
            <FormControl fullWidth>
                <InputLabel id="digital-twin-select-label">Select Simulation Environment</InputLabel>
                <Select
                    labelId="digital-twin-select-label"
                    id="digital-twin-select"
                    value={selectedEnvironment}
                    label="Select Simulation Environment"
                    onChange={onEnvironmentChange}
                >
                    <MenuItem value={"glioblastoma_high_angiogenesis"}>Glioblastoma, High Angiogenesis</MenuItem>
                    <MenuItem value={"pancreatic_cancer_kras_mutant"}>Pancreatic Cancer, KRAS Mutant</MenuItem>
                    <MenuItem value={"healthy_control"}>Healthy Control</MenuItem>
                </Select>
            </FormControl>
        </Box>
    );
};

export default GauntletControl; 
 
 
 
 
 
 
 