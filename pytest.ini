[pytest]
addopts = -ra -q
pythonpath = . oncology-coPilot/oncology-backend
testpaths =
    oncology-coPilot/oncology-backend/tests
markers =
    integration: marks tests as integration tests, which are slower and may require network access. 