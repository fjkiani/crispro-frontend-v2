import React, { useState } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { 
    Shield, ArrowRight, ArrowLeft, Users, Target, TrendingUp,
    Pill, Dna, Clock, CheckCircle, XCircle, Beaker, Activity,
    ChevronRight, Zap, Heart, BarChart3, TestTube, Database,
    Award, Brain, Layers, GitBranch, AlertTriangle, Sparkles
} from 'lucide-react';

//================================================================================
// SLIDE NAVIGATION
//================================================================================

const SlideNav = ({ current, total, onPrev, onNext }) => (
    <div className="fixed bottom-8 left-1/2 -translate-x-1/2 flex items-center gap-6 z-50">
        <button 
            onClick={onPrev}
            disabled={current === 0}
            className="p-3 rounded-full bg-slate-800/80 border border-slate-600 hover:border-cyan-500 disabled:opacity-30 disabled:cursor-not-allowed transition-all"
        >
            <ArrowLeft className="w-5 h-5 text-white"/>
        </button>
        
        <div className="flex gap-2">
            {Array.from({length: total}).map((_, i) => (
                <div 
                    key={i} 
                    className={`w-2 h-2 rounded-full transition-all ${
                        i === current ? 'bg-cyan-400 w-8' : 'bg-slate-600'
                    }`}
                />
            ))}
        </div>
        
        <button 
            onClick={onNext}
            disabled={current === total - 1}
            className="p-3 rounded-full bg-slate-800/80 border border-slate-600 hover:border-cyan-500 disabled:opacity-30 disabled:cursor-not-allowed transition-all"
        >
            <ArrowRight className="w-5 h-5 text-white"/>
        </button>
    </div>
);

//================================================================================
// SLIDE 1: HERO
//================================================================================

const HeroSlide = () => (
    <div className="h-full flex flex-col items-center justify-center text-center px-8">
        <motion.div
            initial={{ opacity: 0, y: 30 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
        >
            <p className="text-cyan-400 text-xl font-semibold tracking-wider mb-4">
                THREE QUESTIONS. ZERO GENERIC ANSWERS.
            </p>
            <h1 className="text-6xl md:text-8xl font-black text-white leading-tight">
                Precision Oncology<br/>
                <span className="text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-purple-500">
                    That Actually Is
                </span>
            </h1>
        </motion.div>
        
        <motion.div 
            className="mt-16 flex flex-col md:flex-row gap-6"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.4 }}
        >
            {[
                { q: "What should I eat?", icon: Beaker, stat: "Drug-specific nutrition" },
                { q: "Will I become resistant?", icon: TrendingUp, stat: "2× risk prediction" },
                { q: "Does this work for me?", icon: Target, stat: "Any cancer type" }
            ].map((item, i) => (
                <div key={i} className="flex flex-col items-center gap-3 px-6 py-4 bg-slate-800/50 border border-slate-700 rounded-xl">
                    <item.icon className="w-8 h-8 text-cyan-400"/>
                    <span className="text-lg text-white font-medium">{item.q}</span>
                    <span className="text-xs text-cyan-300">{item.stat}</span>
                </div>
            ))}
        </motion.div>
        
        <motion.div 
            className="mt-12 grid grid-cols-3 gap-6 max-w-2xl"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.6 }}
        >
            <div className="text-center">
                <p className="text-3xl font-black text-white">469</p>
                <p className="text-sm text-slate-400">Patients Validated</p>
            </div>
            <div className="text-center">
                <p className="text-3xl font-black text-white">100%</p>
                <p className="text-sm text-slate-400">Top-5 Drug Accuracy</p>
            </div>
            <div className="text-center">
                <p className="text-3xl font-black text-white">3</p>
                <p className="text-sm text-slate-400">Validated MOATs</p>
            </div>
        </motion.div>
        
        <motion.p 
            className="mt-8 text-slate-400 text-lg"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.8 }}
        >
            Running in <span className="text-white font-bold">production</span> • 
            Validated on <span className="text-white font-bold">real patient data</span>
        </motion.p>
    </div>
);

//================================================================================
// SLIDE 2: TOXICITY → NUTRITION MOAT (ENHANCED)
//================================================================================

const ToxicitySlide = () => (
    <div className="h-full flex flex-col justify-center px-8 overflow-y-auto">
        <motion.div 
            className="text-center mb-8"
            initial={{ opacity: 0, y: -20 }}
            animate={{ opacity: 1, y: 0 }}
        >
            <div className="inline-flex items-center gap-2 px-4 py-2 bg-cyan-500/10 border border-cyan-500/30 rounded-full mb-4">
                <Shield className="w-5 h-5 text-cyan-400"/>
                <span className="text-cyan-300 font-semibold">MOAT #1: TOXICITY → PROTECTIVE NUTRITION</span>
            </div>
            <h2 className="text-5xl font-black text-white">
                Drug-Specific Nutrition
            </h2>
            <p className="text-xl text-slate-400 mt-3">First system to connect drug toxicity → pathway stress → mitigating foods</p>
        </motion.div>
        
        {/* Enhanced Visual Flow */}
        <motion.div 
            className="flex items-center justify-center gap-3 max-w-6xl mx-auto mb-8"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.3 }}
        >
            {/* Step 1 */}
            <div className="flex-1 p-6 bg-slate-800/50 border border-slate-700 rounded-2xl text-center">
                <Pill className="w-12 h-12 text-purple-400 mx-auto mb-3"/>
                <p className="text-sm text-slate-400 mb-1">Your Drug</p>
                <p className="text-xl font-bold text-white">Carboplatin</p>
                <p className="text-xs text-purple-300 mt-2">Platinum agent</p>
            </div>
            
            <ChevronRight className="w-8 h-8 text-cyan-400 flex-shrink-0"/>
            
            {/* Step 2 */}
            <div className="flex-1 p-6 bg-slate-800/50 border border-slate-700 rounded-2xl text-center">
                <Dna className="w-12 h-12 text-pink-400 mx-auto mb-3"/>
                <p className="text-sm text-slate-400 mb-1">+ Your Gene</p>
                <p className="text-xl font-bold text-white">BRCA1</p>
                <p className="text-xs text-pink-300 mt-2">DNA repair gene</p>
            </div>
            
            <ChevronRight className="w-8 h-8 text-cyan-400 flex-shrink-0"/>
            
            {/* Step 3 */}
            <div className="flex-1 p-6 bg-red-500/10 border border-red-500/30 rounded-2xl text-center">
                <Activity className="w-12 h-12 text-red-400 mx-auto mb-3"/>
                <p className="text-sm text-red-300 mb-1">= Pathway Stress</p>
                <p className="text-xl font-bold text-white">DNA Repair ↓</p>
                <p className="text-xs text-red-300 mt-2">Score: 1.0/1.0</p>
            </div>
            
            <ChevronRight className="w-8 h-8 text-cyan-400 flex-shrink-0"/>
            
            {/* Step 4 */}
            <div className="flex-1 p-6 bg-green-500/10 border border-green-500/30 rounded-2xl text-center">
                <Beaker className="w-12 h-12 text-green-400 mx-auto mb-3"/>
                <p className="text-sm text-green-300 mb-1">→ Protection</p>
                <p className="text-xl font-bold text-white">NAC + Vit D</p>
                <p className="text-xs text-green-300 mt-2">Post-chemo timing</p>
            </div>
        </motion.div>
        
        {/* Technical Details */}
        <motion.div 
            className="grid md:grid-cols-2 gap-6 max-w-5xl mx-auto mb-8"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.5 }}
        >
            <div className="p-6 bg-slate-800/50 border border-slate-700 rounded-2xl">
                <h3 className="text-xl font-bold text-white mb-4 flex items-center gap-2">
                    <Layers className="w-5 h-5 text-cyan-400"/>
                    Three Pathway Categories
                </h3>
                <div className="space-y-3">
                    {[
                        { pathway: "DNA Repair", drugs: "Platinum, PARP", foods: "NAC, Vitamin D, Folate" },
                        { pathway: "Inflammation", drugs: "Checkpoint inhibitors", foods: "Omega-3, Curcumin, EGCG" },
                        { pathway: "Cardiometabolic", drugs: "Anthracyclines", foods: "CoQ10, L-Carnitine, Magnesium" }
                    ].map((item, i) => (
                        <div key={i} className="p-3 bg-slate-900/50 rounded-lg">
                            <p className="text-white font-semibold">{item.pathway}</p>
                            <p className="text-xs text-slate-400 mt-1">Drugs: {item.drugs}</p>
                            <p className="text-xs text-cyan-300 mt-1">Foods: {item.foods}</p>
                        </div>
                    ))}
                </div>
            </div>
            
            <div className="p-6 bg-slate-800/50 border border-slate-700 rounded-2xl">
                <h3 className="text-xl font-bold text-white mb-4 flex items-center gap-2">
                    <Zap className="w-5 h-5 text-purple-400"/>
                    How It Works
                </h3>
                <div className="space-y-3 text-sm text-slate-300">
                    <div className="flex items-start gap-2">
                        <CheckCircle className="w-4 h-4 text-green-400 mt-0.5 flex-shrink-0"/>
                        <span>Detects drug MoA (mechanism of action)</span>
                    </div>
                    <div className="flex items-start gap-2">
                        <CheckCircle className="w-4 h-4 text-green-400 mt-0.5 flex-shrink-0"/>
                        <span>Maps to toxicity pathway (DNA repair, inflammation, etc.)</span>
                    </div>
                    <div className="flex items-start gap-2">
                        <CheckCircle className="w-4 h-4 text-green-400 mt-0.5 flex-shrink-0"/>
                        <span>Checks germline profile (BRCA1, DPYD, etc.)</span>
                    </div>
                    <div className="flex items-start gap-2">
                        <CheckCircle className="w-4 h-4 text-green-400 mt-0.5 flex-shrink-0"/>
                        <span>Calculates pathway stress score</span>
                    </div>
                    <div className="flex items-start gap-2">
                        <CheckCircle className="w-4 h-4 text-green-400 mt-0.5 flex-shrink-0"/>
                        <span>Recommends mitigating foods with timing</span>
                    </div>
                </div>
            </div>
        </motion.div>
        
        {/* Before/After */}
        <motion.div 
            className="grid md:grid-cols-2 gap-6 max-w-4xl mx-auto"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.7 }}
        >
            <div className="p-6 bg-red-500/5 border border-red-500/20 rounded-xl">
                <div className="flex items-center gap-2 mb-3">
                    <XCircle className="w-6 h-6 text-red-400"/>
                    <span className="text-red-300 font-semibold">Everyone Else</span>
                </div>
                <p className="text-xl text-slate-300 italic mb-2">"Eat healthy. Stay hydrated."</p>
                <p className="text-sm text-slate-400">Generic advice, no mechanism alignment</p>
            </div>
            
            <div className="p-6 bg-cyan-500/5 border border-cyan-500/20 rounded-xl">
                <div className="flex items-center gap-2 mb-3">
                    <CheckCircle className="w-6 h-6 text-cyan-400"/>
                    <span className="text-cyan-300 font-semibold">CrisPRO</span>
                </div>
                <p className="text-xl text-white font-medium mb-2">"NAC 600mg post-infusion. Here's why."</p>
                <div className="text-sm text-cyan-300 space-y-1">
                    <p>✓ Carboplatin → DNA repair stress</p>
                    <p>✓ BRCA1 → Increased sensitivity</p>
                    <p>✓ NAC → Glutathione precursor</p>
                    <p>✓ Timing → Post-chemo (not during)</p>
                </div>
            </div>
        </motion.div>
        
        <motion.p 
            className="text-center mt-6 text-cyan-400 font-bold text-lg"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.9 }}
        >
            First to connect Drug → Pathway → Food → Timing • Validated & Production-Ready
        </motion.p>
    </div>
);

//================================================================================
// SLIDE 3: RESISTANCE PREDICTION MOAT (ENHANCED)
//================================================================================

const ResistanceSlide = () => (
    <div className="h-full flex flex-col justify-center px-8 overflow-y-auto">
        <motion.div 
            className="text-center mb-8"
            initial={{ opacity: 0, y: -20 }}
            animate={{ opacity: 1, y: 0 }}
        >
            <div className="inline-flex items-center gap-2 px-4 py-2 bg-purple-500/10 border border-purple-500/30 rounded-full mb-4">
                <TrendingUp className="w-5 h-5 text-purple-400"/>
                <span className="text-purple-300 font-semibold">MOAT #2: RESISTANCE PREDICTION</span>
            </div>
            <h2 className="text-5xl font-black text-white">
                Predict Resistance Before It Happens
            </h2>
            <p className="text-xl text-slate-400 mt-3">Validated on 469 TCGA ovarian cancer patients</p>
        </motion.div>
        
        {/* Enhanced Data Visualization */}
        <motion.div 
            className="grid md:grid-cols-2 gap-8 max-w-6xl mx-auto mb-8"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.3 }}
        >
            {/* Left: The Finding */}
            <div className="p-8 bg-slate-800/50 border border-slate-700 rounded-2xl">
                <h3 className="text-2xl font-bold text-white mb-6 flex items-center gap-2">
                    <BarChart3 className="w-6 h-6 text-purple-400"/>
                    MAPK/NF1 Mutations
                </h3>
                
                <div className="space-y-6">
                    <div>
                        <div className="flex justify-between mb-2">
                            <span className="text-slate-300">With MAPK mutation</span>
                            <span className="text-red-400 font-bold">28.6% resistant</span>
                        </div>
                        <div className="h-6 bg-slate-700 rounded-full overflow-hidden">
                            <div className="h-full bg-gradient-to-r from-red-500 to-red-400 rounded-full flex items-center justify-end pr-2" style={{width: '28.6%'}}>
                                <span className="text-xs text-white font-bold">10/35</span>
                            </div>
                        </div>
                        <p className="text-xs text-slate-400 mt-1">35 patients with MAPK mutations</p>
                    </div>
                    
                    <div>
                        <div className="flex justify-between mb-2">
                            <span className="text-slate-300">Without mutation</span>
                            <span className="text-green-400 font-bold">14.5% resistant</span>
                        </div>
                        <div className="h-6 bg-slate-700 rounded-full overflow-hidden">
                            <div className="h-full bg-gradient-to-r from-green-500 to-green-400 rounded-full flex items-center justify-end pr-2" style={{width: '14.5%'}}>
                                <span className="text-xs text-white font-bold">63/434</span>
                            </div>
                        </div>
                        <p className="text-xs text-slate-400 mt-1">434 patients without MAPK mutations</p>
                    </div>
                </div>
                
                <div className="mt-8 p-4 bg-purple-500/10 border border-purple-500/30 rounded-xl">
                    <div className="text-center">
                        <p className="text-5xl font-black text-purple-400">1.97×</p>
                        <p className="text-slate-300 font-semibold">Relative Risk</p>
                        <p className="text-xs text-slate-400 mt-2">p &lt; 0.05 • Clinically actionable</p>
                    </div>
                </div>
            </div>
            
            {/* Right: The Action */}
            <div className="p-8 bg-gradient-to-br from-purple-500/10 to-cyan-500/10 border border-purple-500/30 rounded-2xl">
                <h3 className="text-2xl font-bold text-white mb-6 flex items-center gap-2">
                    <Target className="w-6 h-6 text-purple-400"/>
                    Clinical Action Plan
                </h3>
                
                <div className="space-y-4">
                    {[
                        { icon: Dna, text: "Detect MAPK/NF1 mutation", detail: "KRAS, NRAS, BRAF, NF1, MAP2K1/2" },
                        { icon: TrendingUp, text: "Flag 2× resistance risk", detail: "RR = 1.97, p < 0.05" },
                        { icon: Clock, text: "Recommend earlier PARP switch", detail: "Before standard progression" },
                        { icon: Activity, text: "Intensify ctDNA monitoring", detail: "Every 6 weeks (not 12)" },
                        { icon: TestTube, text: "Pre-identify ATR/CHK1 trials", detail: "Backup plan ready" }
                    ].map((item, i) => (
                        <motion.div 
                            key={i}
                            className="flex items-start gap-4 p-4 bg-slate-800/50 rounded-xl"
                            initial={{ opacity: 0, x: 20 }}
                            animate={{ opacity: 1, x: 0 }}
                            transition={{ delay: 0.4 + i * 0.1 }}
                        >
                            <div className="p-2 bg-purple-500/20 rounded-lg flex-shrink-0">
                                <item.icon className="w-5 h-5 text-purple-400"/>
                            </div>
                            <div className="flex-1">
                                <span className="text-white font-medium block">{item.text}</span>
                                <span className="text-xs text-slate-400">{item.detail}</span>
                            </div>
                        </motion.div>
                    ))}
                </div>
            </div>
        </motion.div>
        
        {/* Validation Metrics */}
        <motion.div 
            className="grid grid-cols-4 gap-4 max-w-5xl mx-auto"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.8 }}
        >
            <div className="p-4 bg-green-500/10 border border-green-500/30 rounded-xl text-center">
                <p className="text-2xl font-black text-green-400">469</p>
                <p className="text-xs text-slate-300">Patients Validated</p>
            </div>
            <div className="p-4 bg-green-500/10 border border-green-500/30 rounded-xl text-center">
                <p className="text-2xl font-black text-green-400">1.97</p>
                <p className="text-xs text-slate-300">Relative Risk</p>
            </div>
            <div className="p-4 bg-green-500/10 border border-green-500/30 rounded-xl text-center">
                <p className="text-2xl font-black text-green-400">&lt;0.05</p>
                <p className="text-xs text-slate-300">P-Value</p>
            </div>
            <div className="p-4 bg-green-500/10 border border-green-500/30 rounded-xl text-center">
                <p className="text-2xl font-black text-green-400">✓</p>
                <p className="text-xs text-slate-300">Actionable</p>
            </div>
        </motion.div>
        
        {/* Pathway Details */}
        <motion.div 
            className="mt-8 p-6 bg-slate-800/50 border border-slate-700 rounded-2xl max-w-5xl mx-auto"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 1.0 }}
        >
            <h4 className="text-lg font-bold text-white mb-4">MAPK Pathway Genes</h4>
            <div className="grid grid-cols-5 gap-3">
                {['KRAS', 'NRAS', 'BRAF', 'NF1', 'MAP2K1/2'].map((gene, i) => (
                    <div key={i} className="p-3 bg-slate-900/50 rounded-lg text-center">
                        <p className="text-white font-semibold">{gene}</p>
                        <p className="text-xs text-slate-400 mt-1">Signal transducer</p>
                    </div>
                ))}
            </div>
            <p className="text-sm text-slate-400 mt-4 text-center">
                Any mutation in these genes → 2× higher platinum resistance risk
            </p>
        </motion.div>
    </div>
);

//================================================================================
// SLIDE 4: UNIVERSAL CANCER SUPPORT (ENHANCED)
//================================================================================

const UniversalSlide = () => (
    <div className="h-full flex flex-col justify-center px-8 overflow-y-auto">
        <motion.div 
            className="text-center mb-8"
            initial={{ opacity: 0, y: -20 }}
            animate={{ opacity: 1, y: 0 }}
        >
            <div className="inline-flex items-center gap-2 px-4 py-2 bg-emerald-500/10 border border-emerald-500/30 rounded-full mb-4">
                <Target className="w-5 h-5 text-emerald-400"/>
                <span className="text-emerald-300 font-semibold">MOAT #3: UNIVERSAL CANCER SUPPORT</span>
            </div>
            <h2 className="text-5xl font-black text-white">
                One System. Any Cancer.
            </h2>
            <p className="text-xl text-slate-400 mt-3">Indication-agnostic architecture • Same precision for every patient</p>
        </motion.div>
        
        {/* Cancer Types Grid - Enhanced */}
        <motion.div 
            className="grid grid-cols-5 gap-4 max-w-5xl mx-auto mb-8"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.3 }}
        >
            {[
                { name: "Ovarian", color: "from-pink-500 to-rose-500", soc: "Carboplatin + Paclitaxel" },
                { name: "Breast", color: "from-purple-500 to-pink-500", soc: "AC-T" },
                { name: "Colorectal", color: "from-blue-500 to-cyan-500", soc: "FOLFOX/FOLFIRI" },
                { name: "Melanoma", color: "from-amber-500 to-orange-500", soc: "Pembrolizumab + BRAF/MEK" },
                { name: "Myeloma", color: "from-emerald-500 to-teal-500", soc: "VRd" }
            ].map((cancer, i) => (
                <motion.div 
                    key={i}
                    className="p-6 bg-slate-800/50 border border-slate-700 rounded-2xl text-center"
                    initial={{ opacity: 0, scale: 0.8 }}
                    animate={{ opacity: 1, scale: 1 }}
                    transition={{ delay: 0.4 + i * 0.1 }}
                >
                    <div className={`w-16 h-16 mx-auto mb-3 rounded-2xl bg-gradient-to-br ${cancer.color} flex items-center justify-center`}>
                        <Heart className="w-8 h-8 text-white"/>
                    </div>
                    <p className="text-white font-semibold mb-2">{cancer.name}</p>
                    <p className="text-xs text-slate-400">{cancer.soc}</p>
                </motion.div>
            ))}
        </motion.div>
        
        {/* Universal Biomarker Intelligence */}
        <motion.div 
            className="grid md:grid-cols-2 gap-6 max-w-5xl mx-auto mb-8"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.6 }}
        >
            <div className="p-6 bg-slate-800/50 border border-slate-700 rounded-2xl">
                <h3 className="text-xl font-bold text-white mb-4 flex items-center gap-2">
                    <Database className="w-5 h-5 text-emerald-400"/>
                    Universal Biomarkers
                </h3>
                <div className="space-y-2 text-sm">
                    {[
                        { marker: "CA-125", cancer: "Ovarian" },
                        { marker: "PSA", cancer: "Prostate" },
                        { marker: "CEA", cancer: "Colorectal" },
                        { marker: "AFP", cancer: "Liver" },
                        { marker: "hCG", cancer: "Germ cell" }
                    ].map((item, i) => (
                        <div key={i} className="flex justify-between p-2 bg-slate-900/50 rounded">
                            <span className="text-white">{item.marker}</span>
                            <span className="text-emerald-300">{item.cancer}</span>
                        </div>
                    ))}
                </div>
            </div>
            
            <div className="p-6 bg-slate-800/50 border border-slate-700 rounded-2xl">
                <h3 className="text-xl font-bold text-white mb-4 flex items-center gap-2">
                    <GitBranch className="w-5 h-5 text-cyan-400"/>
                    Profile Adapter
                </h3>
                <div className="space-y-3 text-sm text-slate-300">
                    <div className="flex items-start gap-2">
                        <CheckCircle className="w-4 h-4 text-green-400 mt-0.5 flex-shrink-0"/>
                        <span>Accepts simple profiles: <code className="text-cyan-300">{"{patient_id, disease, biomarkers}"}</code></span>
                    </div>
                    <div className="flex items-start gap-2">
                        <CheckCircle className="w-4 h-4 text-green-400 mt-0.5 flex-shrink-0"/>
                        <span>Accepts full profiles: Complete mutation data, treatment history</span>
                    </div>
                    <div className="flex items-start gap-2">
                        <CheckCircle className="w-4 h-4 text-green-400 mt-0.5 flex-shrink-0"/>
                        <span>Auto-detects cancer type from biomarkers</span>
                    </div>
                    <div className="flex items-start gap-2">
                        <CheckCircle className="w-4 h-4 text-green-400 mt-0.5 flex-shrink-0"/>
                        <span>Applies disease-specific SOC recommendations</span>
                    </div>
                </div>
            </div>
        </motion.div>
        
        {/* Single Endpoint */}
        <motion.div 
            className="max-w-4xl mx-auto p-8 bg-gradient-to-r from-emerald-500/10 to-cyan-500/10 border border-emerald-500/30 rounded-2xl"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.8 }}
        >
            <div className="text-center mb-6">
                <code className="text-xl bg-slate-800 px-6 py-3 rounded-lg text-emerald-400 font-mono block mb-4">
                    POST /api/complete_care/universal
                </code>
                <p className="text-slate-300 text-lg">
                    One API call → Complete care plan for <span className="text-white font-bold">any patient</span>
                </p>
            </div>
            
            <div className="grid grid-cols-3 gap-4 mt-6">
                {[
                    { label: "Cancer Types", value: "5+" },
                    { label: "Biomarkers", value: "5" },
                    { label: "SOC Regimens", value: "5" }
                ].map((item, i) => (
                    <div key={i} className="text-center p-4 bg-slate-900/50 rounded-lg">
                        <p className="text-2xl font-black text-emerald-400">{item.value}</p>
                        <p className="text-xs text-slate-400 mt-1">{item.label}</p>
                    </div>
                ))}
            </div>
        </motion.div>
    </div>
);

//================================================================================
// SLIDE 5: TECHNICAL VALIDATION (NEW)
//================================================================================

const TechnicalSlide = () => (
    <div className="h-full flex flex-col justify-center px-8 overflow-y-auto">
        <motion.div 
            className="text-center mb-8"
            initial={{ opacity: 0, y: -20 }}
            animate={{ opacity: 1, y: 0 }}
        >
            <div className="inline-flex items-center gap-2 px-4 py-2 bg-blue-500/10 border border-blue-500/30 rounded-full mb-4">
                <Award className="w-5 h-5 text-blue-400"/>
                <span className="text-blue-300 font-semibold">VALIDATION METRICS</span>
            </div>
            <h2 className="text-5xl font-black text-white">
                Proven Performance
            </h2>
            <p className="text-xl text-slate-400 mt-3">Real data. Real results. Production-ready.</p>
        </motion.div>
        
        {/* Validation Grid */}
        <motion.div 
            className="grid md:grid-cols-2 gap-6 max-w-6xl mx-auto mb-8"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.3 }}
        >
            {/* Drug Ranking */}
            <div className="p-6 bg-slate-800/50 border border-slate-700 rounded-2xl">
                <h3 className="text-xl font-bold text-white mb-4 flex items-center gap-2">
                    <Target className="w-5 h-5 text-blue-400"/>
                    Drug Ranking Accuracy
                </h3>
                <div className="space-y-4">
                    <div>
                        <div className="flex justify-between mb-2">
                            <span className="text-slate-300">Top-5 Accuracy</span>
                            <span className="text-green-400 font-bold">100%</span>
                        </div>
                        <div className="h-4 bg-slate-700 rounded-full overflow-hidden">
                            <div className="h-full bg-gradient-to-r from-green-500 to-green-400 rounded-full" style={{width: '100%'}}/>
                        </div>
                        <p className="text-xs text-slate-400 mt-1">17/17 patients, correct drugs in top 5</p>
                    </div>
                    <div className="p-3 bg-slate-900/50 rounded-lg">
                        <p className="text-sm text-slate-300">S/P/E Framework:</p>
                        <p className="text-xs text-cyan-300 mt-1">30% Sequence + 40% Pathway + 30% Evidence</p>
                    </div>
                </div>
            </div>
            
            {/* Pathway Computation */}
            <div className="p-6 bg-slate-800/50 border border-slate-700 rounded-2xl">
                <h3 className="text-xl font-bold text-white mb-4 flex items-center gap-2">
                    <Brain className="w-5 h-5 text-purple-400"/>
                    Pathway Computation
                </h3>
                <div className="space-y-3">
                    {[
                        { pathway: "DDR", score: "1.0/1.0", status: "MAXIMUM" },
                        { pathway: "TP53", score: "0.8/1.0", status: "HIGH" },
                        { pathway: "MAPK", score: "0.6/1.0", status: "MODERATE" }
                    ].map((item, i) => (
                        <div key={i} className="flex justify-between items-center p-3 bg-slate-900/50 rounded-lg">
                            <span className="text-white font-semibold">{item.pathway}</span>
                            <div className="text-right">
                                <span className="text-cyan-300 font-bold">{item.score}</span>
                                <p className="text-xs text-slate-400">{item.status}</p>
                            </div>
                        </div>
                    ))}
                </div>
            </div>
        </motion.div>
        
        {/* Mechanism Vectors */}
        <motion.div 
            className="p-6 bg-slate-800/50 border border-slate-700 rounded-2xl max-w-4xl mx-auto mb-8"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.5 }}
        >
            <h3 className="text-xl font-bold text-white mb-4 flex items-center gap-2">
                <Layers className="w-5 h-5 text-cyan-400"/>
                7D Mechanism Vector
            </h3>
            <div className="grid grid-cols-7 gap-2">
                {['DDR', 'MAPK', 'PI3K', 'VEGF', 'HER2', 'IO', 'Efflux'].map((pathway, i) => (
                    <div key={i} className="p-3 bg-slate-900/50 rounded-lg text-center">
                        <p className="text-xs text-slate-400 mb-1">{pathway}</p>
                        <div className="h-16 bg-slate-700 rounded relative overflow-hidden">
                            <div 
                                className="absolute bottom-0 w-full bg-gradient-to-t from-cyan-500 to-cyan-400 rounded"
                                style={{height: `${(i === 0 ? 88 : i === 1 ? 12 : i === 2 ? 15 : 10) * 0.8}%`}}
                            />
                            <span className="absolute inset-0 flex items-center justify-center text-xs font-bold text-white">
                                {i === 0 ? '0.88' : i === 1 ? '0.12' : i === 2 ? '0.15' : '0.10'}
                            </span>
                        </div>
                    </div>
                ))}
            </div>
            <p className="text-sm text-slate-400 mt-4 text-center">
                Used for mechanism-based trial matching • DDR: 0.88 (high burden) → PARP trials rank higher
            </p>
        </motion.div>
        
        {/* Production Status */}
        <motion.div 
            className="flex justify-center gap-4 max-w-4xl mx-auto"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.7 }}
        >
            {[
                { label: "Production", value: "✅", color: "green" },
                { label: "Validated", value: "469", color: "blue" },
                { label: "Accuracy", value: "100%", color: "cyan" },
                { label: "MOATs", value: "3", color: "purple" }
            ].map((item, i) => (
                <div key={i} className={`p-4 bg-${item.color}-500/10 border border-${item.color}-500/30 rounded-xl text-center flex-1`}>
                    <p className={`text-2xl font-black text-${item.color}-400`}>{item.value}</p>
                    <p className="text-xs text-slate-300 mt-1">{item.label}</p>
                </div>
            ))}
        </motion.div>
    </div>
);

//================================================================================
// SLIDE 6: COMPETITIVE ADVANTAGE (NEW)
//================================================================================

const CompetitiveSlide = () => (
    <div className="h-full flex flex-col justify-center px-8 overflow-y-auto">
        <motion.div 
            className="text-center mb-8"
            initial={{ opacity: 0, y: -20 }}
            animate={{ opacity: 1, y: 0 }}
        >
            <div className="inline-flex items-center gap-2 px-4 py-2 bg-amber-500/10 border border-amber-500/30 rounded-full mb-4">
                <Sparkles className="w-5 h-5 text-amber-400"/>
                <span className="text-amber-300 font-semibold">THE COMPETITIVE MOAT</span>
            </div>
            <h2 className="text-5xl font-black text-white">
                What Only We Can Do
            </h2>
            <p className="text-xl text-slate-400 mt-3">Three validated capabilities no competitor has</p>
        </motion.div>
        
        {/* Comparison Table */}
        <motion.div 
            className="max-w-6xl mx-auto mb-8"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.3 }}
        >
            <div className="overflow-x-auto">
                <table className="w-full border-collapse">
                    <thead>
                        <tr className="border-b border-slate-700">
                            <th className="text-left p-4 text-slate-300 font-semibold">Capability</th>
                            <th className="text-center p-4 text-red-300 font-semibold">Competitors</th>
                            <th className="text-center p-4 text-cyan-300 font-semibold">CrisPRO</th>
                        </tr>
                    </thead>
                    <tbody>
                        {[
                            { 
                                capability: "Toxicity → Nutrition", 
                                competitor: "Eat healthy. Stay hydrated.", 
                                crispro: "NAC 600mg post-infusion. Here's why." 
                            },
                            { 
                                capability: "Resistance Prediction", 
                                competitor: "Monitor for progression", 
                                crispro: "MAPK mutation = 2× risk. Action plan ready." 
                            },
                            { 
                                capability: "Universal Support", 
                                competitor: "Disease-specific tools", 
                                crispro: "One system, any cancer type" 
                            },
                            { 
                                capability: "Evidence Basis", 
                                competitor: "General literature", 
                                crispro: "469 real patients, validated RR = 2.0" 
                            },
                            { 
                                capability: "Actionability", 
                                competitor: "Generic advice", 
                                crispro: "Specific treatment modifications" 
                            },
                            { 
                                capability: "Personalization", 
                                competitor: "One-size-fits-all", 
                                crispro: "Based on YOUR mutations" 
                            }
                        ].map((row, i) => (
                            <motion.tr 
                                key={i}
                                className="border-b border-slate-800"
                                initial={{ opacity: 0, x: -20 }}
                                animate={{ opacity: 1, x: 0 }}
                                transition={{ delay: 0.4 + i * 0.1 }}
                            >
                                <td className="p-4 text-white font-medium">{row.capability}</td>
                                <td className="p-4 text-center">
                                    <div className="inline-flex items-center gap-2 px-3 py-1 bg-red-500/10 border border-red-500/30 rounded-lg">
                                        <XCircle className="w-4 h-4 text-red-400"/>
                                        <span className="text-sm text-red-300">{row.competitor}</span>
                                    </div>
                                </td>
                                <td className="p-4 text-center">
                                    <div className="inline-flex items-center gap-2 px-3 py-1 bg-cyan-500/10 border border-cyan-500/30 rounded-lg">
                                        <CheckCircle className="w-4 h-4 text-cyan-400"/>
                                        <span className="text-sm text-cyan-300">{row.crispro}</span>
                                    </div>
                                </td>
                            </motion.tr>
                        ))}
                    </tbody>
                </table>
            </div>
        </motion.div>
        
        {/* The Three Questions */}
        <motion.div 
            className="grid md:grid-cols-3 gap-6 max-w-5xl mx-auto"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.9 }}
        >
            {[
                {
                    question: "What should I eat?",
                    before: "Eat healthy. Stay hydrated.",
                    after: "NAC 600mg post-infusion. Your carboplatin + BRCA1 = DNA repair stress. NAC helps.",
                    moat: "Toxicity → Nutrition"
                },
                {
                    question: "Will I become resistant?",
                    before: "We'll monitor and see.",
                    after: "Your MAPK mutation = 2× resistance risk. Earlier PARP switch recommended.",
                    moat: "Resistance Prediction"
                },
                {
                    question: "Does this work for me?",
                    before: "Try this drug (generic).",
                    after: "Works for any cancer. Same precision. One system.",
                    moat: "Universal Support"
                }
            ].map((item, i) => (
                <div key={i} className="p-6 bg-slate-800/50 border border-slate-700 rounded-2xl">
                    <h4 className="text-lg font-bold text-white mb-3">{item.question}</h4>
                    <div className="space-y-3">
                        <div className="p-3 bg-red-500/5 border border-red-500/20 rounded-lg">
                            <p className="text-xs text-red-300 mb-1">Before:</p>
                            <p className="text-sm text-slate-300 italic">{item.before}</p>
                        </div>
                        <div className="p-3 bg-cyan-500/5 border border-cyan-500/20 rounded-lg">
                            <p className="text-xs text-cyan-300 mb-1">After:</p>
                            <p className="text-sm text-white">{item.after}</p>
                        </div>
                        <div className="mt-3 pt-3 border-t border-slate-700">
                            <p className="text-xs text-amber-300 font-semibold">{item.moat}</p>
                        </div>
                    </div>
                </div>
            ))}
        </motion.div>
    </div>
);

//================================================================================
// SLIDE 7: SUMMARY (ENHANCED)
//================================================================================

const SummarySlide = () => (
    <div className="h-full flex flex-col justify-center px-8">
        <motion.div 
            className="text-center mb-12"
            initial={{ opacity: 0, y: -20 }}
            animate={{ opacity: 1, y: 0 }}
        >
            <h2 className="text-5xl font-black text-white mb-4">
                The Bottom Line
            </h2>
            <p className="text-xl text-slate-400">
                Three capabilities. All validated. All in production.
            </p>
        </motion.div>
        
        {/* Enhanced Summary Cards */}
        <motion.div 
            className="grid md:grid-cols-3 gap-6 max-w-6xl mx-auto mb-12"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.3 }}
        >
            {[
                { 
                    icon: Shield, 
                    title: "Toxicity → Nutrition",
                    answer: "NAC for YOUR stress",
                    color: "cyan",
                    validation: "Drug-specific, germline-aware",
                    example: "Carboplatin + BRCA1 → NAC post-chemo"
                },
                { 
                    icon: TrendingUp, 
                    title: "Resistance Prediction",
                    answer: "2× risk = action plan",
                    color: "purple",
                    validation: "469 patients, RR = 1.97, p < 0.05",
                    example: "MAPK mutation → Earlier PARP switch"
                },
                { 
                    icon: Target, 
                    title: "Universal Support",
                    answer: "Any cancer, same precision",
                    color: "emerald",
                    validation: "5+ cancer types, 5 biomarkers",
                    example: "One endpoint, any patient profile"
                }
            ].map((item, i) => (
                <motion.div 
                    key={i}
                    className="p-6 bg-slate-800/50 border border-slate-700 rounded-2xl"
                    initial={{ opacity: 0, y: 20 }}
                    animate={{ opacity: 1, y: 0 }}
                    transition={{ delay: 0.4 + i * 0.1 }}
                >
                    <div className="flex items-center gap-3 mb-4">
                        <div className={`p-3 bg-${item.color}-500/20 rounded-lg`}>
                            <item.icon className={`w-6 h-6 text-${item.color}-400`}
                                style={{ color: item.color === 'cyan' ? '#22d3ee' : item.color === 'purple' ? '#a855f7' : '#34d399' }}
                            />
                        </div>
                        <h3 className="text-xl font-bold text-white">{item.title}</h3>
                    </div>
                    <p className="text-slate-300 mb-3">{item.answer}</p>
                    <div className="p-3 bg-slate-900/50 rounded-lg mb-3">
                        <p className="text-xs text-slate-400 mb-1">Validation:</p>
                        <p className="text-sm text-white">{item.validation}</p>
                    </div>
                    <div className="p-3 bg-slate-900/50 rounded-lg">
                        <p className="text-xs text-slate-400 mb-1">Example:</p>
                        <p className="text-sm text-cyan-300">{item.example}</p>
                    </div>
                </motion.div>
            ))}
        </motion.div>
        
        {/* Key Metrics */}
        <motion.div 
            className="grid grid-cols-4 gap-4 max-w-5xl mx-auto mb-8"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.7 }}
        >
            {[
                { metric: "469", label: "Patients Validated", icon: Database },
                { metric: "100%", label: "Top-5 Accuracy", icon: Award },
                { metric: "2.0×", label: "Resistance RR", icon: TrendingUp },
                { metric: "3", label: "Validated MOATs", icon: Sparkles }
            ].map((item, i) => (
                <div key={i} className="p-4 bg-slate-800/50 border border-slate-700 rounded-xl text-center">
                    <item.icon className="w-6 h-6 text-cyan-400 mx-auto mb-2"/>
                    <p className="text-2xl font-black text-white">{item.metric}</p>
                    <p className="text-xs text-slate-400 mt-1">{item.label}</p>
                </div>
            ))}
        </motion.div>
        
        {/* Final CTA */}
        <motion.div 
            className="text-center"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.9 }}
        >
            <p className="text-2xl text-white max-w-4xl mx-auto mb-4">
                Not theoretical. <span className="text-cyan-400 font-bold">Validated on real patient data.</span>
            </p>
            <p className="text-xl text-slate-400">
                Every oncologist wants these answers. <span className="text-white font-semibold">We're the only ones who built them.</span>
            </p>
            <div className="mt-8 flex justify-center gap-4">
                <div className="px-4 py-2 bg-green-500/20 border border-green-500/30 rounded-lg">
                    <span className="text-green-300 font-semibold">✓ Production-Ready</span>
                </div>
                <div className="px-4 py-2 bg-green-500/20 border border-green-500/30 rounded-lg">
                    <span className="text-green-300 font-semibold">✓ Clinically Validated</span>
                </div>
                <div className="px-4 py-2 bg-green-500/20 border border-green-500/30 rounded-lg">
                    <span className="text-green-300 font-semibold">✓ Evidence-Backed</span>
                </div>
            </div>
        </motion.div>
    </div>
);

//================================================================================
// MAIN APP WITH SLIDE CONTROL
//================================================================================

export default function App() {
    const [currentSlide, setCurrentSlide] = useState(0);
    
    const slides = [
        { id: 'hero', component: HeroSlide },
        { id: 'toxicity', component: ToxicitySlide },
        { id: 'resistance', component: ResistanceSlide },
        { id: 'universal', component: UniversalSlide },
        { id: 'technical', component: TechnicalSlide },
        { id: 'competitive', component: CompetitiveSlide },
        { id: 'summary', component: SummarySlide }
    ];
    
    const nextSlide = () => setCurrentSlide(prev => Math.min(prev + 1, slides.length - 1));
    const prevSlide = () => setCurrentSlide(prev => Math.max(prev - 1, 0));
    
    // Keyboard navigation
    React.useEffect(() => {
        const handleKeyDown = (e) => {
            if (e.key === 'ArrowRight' || e.key === ' ') nextSlide();
            if (e.key === 'ArrowLeft') prevSlide();
        };
        window.addEventListener('keydown', handleKeyDown);
        return () => window.removeEventListener('keydown', handleKeyDown);
    }, []);
    
    const CurrentSlideComponent = slides[currentSlide].component;
    
    return (
        <main className="relative w-full h-screen bg-slate-900 font-sans overflow-hidden">
            {/* Background */}
            <div className="absolute inset-0 bg-gradient-to-br from-slate-900 via-slate-800 to-black/40 z-0"/>
            
            {/* Slide Content */}
            <div className="relative z-10 h-full">
                <AnimatePresence mode="wait">
                    <motion.div
                        key={currentSlide}
                        initial={{ opacity: 0, x: 50 }}
                        animate={{ opacity: 1, x: 0 }}
                        exit={{ opacity: 0, x: -50 }}
                        transition={{ duration: 0.3 }}
                        className="h-full"
                    >
                        <CurrentSlideComponent />
                    </motion.div>
                </AnimatePresence>
            </div>
            
            {/* Navigation */}
            <SlideNav 
                current={currentSlide} 
                total={slides.length}
                onPrev={prevSlide}
                onNext={nextSlide}
            />
        </main>
    );
}
